import { NS } from "@ns";

export async function main(ns: NS): Promise<void> {
  // get number of required open ports
  // check current number of open ports
  // if open ports is less than required ports
  // run brutessh
  // if still not enough ports
  // throw error
  // run nuke to get root access
}
