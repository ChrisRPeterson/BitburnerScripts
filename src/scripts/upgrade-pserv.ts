import { NS } from "@ns";
import { HOME_HOST } from "/lib/constants";

// const upgradeRam = 16

// export async function main(ns: NS): Promise<void> {
//   const pservs = ns.getPurchasedServers();
//   pservs.forEach((pserv) => {
//     if (ns.ram)
//       if (ns.getServerMoneyAvailable(HOME_HOST) > ns.getPurchasedServerUpgradeCost(pserv, 16)) {

//       }
//   })
// }
